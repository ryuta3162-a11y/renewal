/** 選択枠調整（将来用）。現状は triggerLayout を呼ばず描画を壊さない */
export function refitZoneGroupBounds(group) {
  return group;
}

export function collapseEdgeDimMarkers(group) {
  return group;
}

export function restoreEdgeDimMarkers(group) {
  return group;
}
